# Copia in .env e riempi con i valori del tuo progetto Supabase
# (Project Settings → API). La anon key è pubblica per progetto: finisce
# comunque dentro l'app, ed è sicura solo perché le RLS la limitano.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
